# OSRM foundation

OSRM is self-hosted and opt-in because this repository does not ship a large map graph. Without
the `routing` profile, readiness reports `not_configured` and the rest of the local stack remains
usable. See [data/README.md](data/README.md) before enabling it.

